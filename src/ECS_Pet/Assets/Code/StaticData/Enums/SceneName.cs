namespace Code.StaticData
{
    public enum SceneName
    {
        // Enum should have same spelling as scene name, because it will be parse to string
        
        Bootstrap = 0,
        EcsWorld = 1
        
        // last index = 1
    }
}